services:
  - type: web
    name: deck-sop-agent
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm run web
    envVars:
      - key: SITE_PASSWORD
        sync: false
