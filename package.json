{
  "name": "deck-sop-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone AI agent that builds HiveMinds-format PowerPoint decks using the deck-sop skill and the Claude Agent SDK.",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "bin": {
    "deck-agent": "./src/build-deck.mjs"
  },
  "scripts": {
    "build-deck": "node src/build-deck.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.220",
    "@anthropic-ai/sdk": "^0.93.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "pptxgenjs": "^3.12.0",
    "zod": "^4.0.0"
  }
}
